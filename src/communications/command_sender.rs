use domain::team_type::TeamType;
use domain::command::Command;

pub struct CommandSender;

impl CommandSender {

    pub fn create_socket(&self, _team_type: TeamType) {

    }

    pub fn send_command(&self, _command: Command) {

    }
}